/**
 * UI module switches (code-only).
 *
 * Keep these toggles centralized so modules can be enabled/disabled with a
 * single-line conditional in their host layout components.
 */
export const UI_MODULES = {
  bottomMenu: true,
} as const;
